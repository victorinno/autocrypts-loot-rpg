/** Modernist Dungeon Ledger: the full-screen canvas and expedition desk are the sole route. */

import GameCanvas from "@/components/GameCanvas";

export default function App() {
  return <GameCanvas />;
}
